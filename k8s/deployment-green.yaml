apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web
      variant: green
  template:
    metadata:
      labels:
        app: web
        variant: green
    spec:
      terminationGracePeriodSeconds: 5
      containers:
        - name: web-server
          image: hello-k8s-pixie-web
          imagePullPolicy: Never
          env:
            - name: VARIANT
              value: "green"
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 1
            periodSeconds: 5
